{
  "name": "web-app-draken",
  "private": true,
  "scripts": {
    "dragon": "node scripts/dragon.mjs",
    "dragon:release": "node scripts/dragon-release.mjs",
    "prepare": "husky",
    "type-check": "yarn --cwd frontend type-check && yarn --cwd frontend type-check:test && yarn --cwd backend type-check && yarn --cwd backend type-check:test",
    "lint:logging": "node scripts/check-runtime-logging.mjs",
    "lint": "yarn --cwd frontend lint && yarn --cwd frontend lint:deps && yarn --cwd backend lint",
    "knip": "yarn --cwd frontend knip && yarn --cwd backend knip",
    "format": "yarn --cwd frontend format && yarn --cwd backend format",
    "test": "yarn test:architecture && yarn --cwd frontend test && yarn --cwd backend test",
    "verify": "yarn type-check && yarn lint:strict && yarn format:check && yarn test",
    "test:architecture": "node --test scripts/*.test.mjs",
    "lint:strict": "yarn --cwd frontend lint:strict && yarn --cwd frontend lint:deps && yarn --cwd backend lint:strict",
    "format:check": "yarn --cwd frontend format:check && yarn --cwd backend format:check"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3"
  }
}
