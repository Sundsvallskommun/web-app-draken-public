{
  // Check colocated unit tests without depending on generated Next build types.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "incremental": false,
    "noEmit": true
  },
  "include": ["src/**/*.test.ts", "vitest.config.mts", "next-env.d.ts"]
}
